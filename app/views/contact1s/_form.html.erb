<%= form_with(model: contact1, local: true) do |form| %>
  <% if contact1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact1.errors.count, "error") %> prohibited this contact1 from being saved:</h2>

      <ul>
      <% contact1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :correo %>
    <%= form.text_field :correo %>
  </div>

  <div class="field">
    <%= form.label :asunto %>
    <%= form.text_field :asunto %>
  </div>

  <div class="field">
    <%= form.label :mensaje %>
    <%= form.text_area :mensaje %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
