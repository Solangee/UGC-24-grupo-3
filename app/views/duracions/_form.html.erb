<%= form_with(model: duracion, local: true) do |form| %>
  <% if duracion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(duracion.errors.count, "error") %> prohibited this duracion from being saved:</h2>

      <ul>
      <% duracion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
